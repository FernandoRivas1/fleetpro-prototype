# Base URL of the FastAPI backend. Defaults to http://localhost:8000 in
# code (see src/lib/api.ts) if this isn't set, so a plain `npm run dev`
# works with zero config against a locally-running backend.
VITE_API_BASE_URL=http://localhost:8000
